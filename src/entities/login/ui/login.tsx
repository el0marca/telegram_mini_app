import { useEffect } from "react";
// import { useSetCityMutation } from "../model/authApi";

export const Login = () => {
  // const [getToken, { data, isFetching }] = useGetOrUpdateTokenMutation();
  // const [setCity, { data: as, isFetching: a }] = useSetCityMutation();

  useEffect(() => {
    // getToken({ nullify_push: false, token: null });
    // setCity({ city_brach_id: "f", companyId: "" });
  }, []);

  // console.log(data);
  return <>login</>;
};
